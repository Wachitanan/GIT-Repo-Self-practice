ทำความเข้าใจเกี่ยวกับการใช้ toLowerCase และ split โดย toLowerCase คือการทำให้ตัวที่เราต้องการเป็นตัวพิมพ์เล็กทั้งหมดโดยใช้คำสั่ง .toLowerCase()
และ .split(" ") คือการแยก str ให้กลายเป็น arryaของคำ ex. ถ้าเรามีคำว่า Hello มันจะเก็บเป็น ("H","e","l","l","o")
reduce เป็นการวนลูปทีละคำและสะสมค่าไปเรื่อยๆ เช่น reduce((arr,cur)) โดย acc คือตัวสะสมค่าเริ่มต้นคือ {} เปล่า และ cur คือค่าปัจจุบันที่กำลังวน